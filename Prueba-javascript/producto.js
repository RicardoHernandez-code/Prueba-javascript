class Producto{
	constructor(numPedido,ref,marca,peso,precio){
		this.numPedido = numPedido;
		this.ref = ref;
		this.marca = marca;
		this.peso = parseFloat(peso);
		this.precio = parseFloat(precio);
	}
}